const request = require('request'),
      { OAuth2Client } = require('google-auth-library'),
      client = new OAuth2Client("Y9ISJ0883boDPyQSQcAMof_H");
      require('dotenv').config()

class GuiController {
  static home (req, res) {
  }
}

module.exports = GuiController